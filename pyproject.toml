[project]
name = "csce464-midterm"
version = "0.1.0"
description = "Session Buddy — a presiding officer helper for congressional debate"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "mysql-connector-python",
]
